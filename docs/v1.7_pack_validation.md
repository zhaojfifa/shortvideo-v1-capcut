基础校验（必过）

 manifest.json 存在且可解析

 raw/raw.mp4 存在

 subs/my.srt 存在

TTS 条件校验（当 tts=true）

 audio/voice_my.wav 存在

 audio/voice_my.wav 文件大小 > 0

非校验项（明确不做）

不校验字幕语义

不校验字幕时间轴

不校验音画同步程度

不校验 scenes 内容

结论性规则：
满足“基础校验 +（若 tts=true）TTS 条件校验” ⇒ Pack 合格。