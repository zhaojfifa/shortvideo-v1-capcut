# v1.9 ApolloAvatar PR-3 (UI isolation + UTF-8 guard)

## 0. Goal
- Fix `/tasks/apollo-avatar/new` 500 caused by non-UTF8 Jinja templates on Render.
- Keep existing Suitcase pipeline untouched (non-interference).
- ApolloAvatar becomes an isolated "scene" with:
  - dedicated list page (only apollo_avatar tasks)
  - dedicated new task page
  - future: video gen -> translate -> dub -> pack (still using baseline pipeline as foundation)

## 1. Non-interference rules (must)
- DO NOT modify existing Suitcase `/tasks/new` UI/flow.
- DO NOT change existing `/api/tasks` semantics.
- ApolloAvatar routes/pages must be additive and gated by `ENABLE_APOLLO_AVATAR`.

## 2. ApolloAvatar UI
### 2.1 List page
- Entry: Tasks board tab "数字人"
- URL: `/tasks?kind=apollo_avatar`
- List must show ONLY `kind=apollo_avatar` tasks.
- "任务" tab in this board must be renamed to "热点跟随" (placeholder, empty).
- Tools tab unchanged.

### 2.2 New page
- URL: `/tasks/apollo-avatar/new`
- Must not link back to Suitcase `/tasks/new`.
- Default demo assets:
  - image: `/static/demo/demo_avatar.jpg`
  - ref video: `/static/demo/demo_ref.mp4`
- Add seed field (optional), passed through to backend task meta.

## 3. Encoding guard
- All `gateway/app/templates/**/*.html` must be UTF-8 (no BOM).
- Add a script `gateway/app/scripts/check_templates_utf8.py` to fail fast if non-UTF8 exists.

## 4. Acceptance checklist
- [ ] `/tasks/apollo-avatar/new` renders (no 500) on Render.
- [ ] `/tasks?kind=apollo_avatar` shows only apollo_avatar tasks.
- [ ] Suitcase `/tasks/new` unchanged.
- [ ] UTF-8 guard script passes.
